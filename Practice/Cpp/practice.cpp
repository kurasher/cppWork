#include <iostream>
#include <string>

using namespace std;

int main(){
    int first, second;
    cin >> first;
    cin >> second;
    cout << first << " + " << second << " = " << first + second << endl;
    cout << first << " - " << second << " = " << first - second << endl;
    return 0;
}
